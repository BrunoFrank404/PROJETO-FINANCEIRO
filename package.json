{
  "name": "backend-gastos",
  "version": "1.0.0",
  "description": "Backend para gerenciamento de gastos mensais",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "postinstall": "npm rebuild sqlite3 --build-from-source || true && npm --prefix Frontend ci && npm --prefix Frontend run build",
    "build:frontend": "npm --prefix Frontend ci && npm --prefix Frontend run build",
    "build": "npm run build:frontend"
  },
  "dependencies": {
    "express": "^4.18.2",
    "sqlite3": "^5.1.6",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}